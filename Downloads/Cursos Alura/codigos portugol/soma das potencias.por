programa {
  funcao inicio() {
    inteiro a, b, poa, pob, soma

    escreva("primeiro numero: ")
    leia(a)

    escreva("segundo numero: ")
    leia(b)

    poa = a * a
    pob = b * b
    soma = poa + pob

    escreva("a soma das potencias é: ", soma)
  }
}
